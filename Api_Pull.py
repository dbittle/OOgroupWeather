class Api_Pull:


	def __init__(self, weather_source, city, state):
		self.weather_source = weather_source
		self.city = city
		self.state = state

	def get_weather_source(self): return
	def query_API(self): return
	def getJSONData(self): return 
